import styled from 'styled-components';

export const FormContainer = styled('form')({
  display: 'flex',
  flexDirection: 'column',
  gap: '0.8rem',
  margin: '1.6rem 0',
  padding: '0',
  maxWidth: 620
});
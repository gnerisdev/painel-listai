import styled from 'styled-components';

export const Header = styled('header')({
  width: '100%',
  height: 60,
  position: 'fixed',
  inset: 0,
  padding: '10px 0',
  background: '#1e242d',
  borderBottom: '1px solid #1e242d',
  position: 'fixed',
  zIndex: 9,
});